name: release

on:
  workflow_dispatch:
    inputs:
      semver:
        description: "The semver to use"
        required: true
        default: "patch"
      tag:
        description: "The npm tag"
        required: false
        default: "latest"
  pull_request:
    types: [closed]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Set env
        shell: bash
        run: echo "USER_NPM_TOKEN=$(jq 'to_entries|map("\(.key)=\(.value|tostring)")|.[]' <(echo \"${{ toJson(secrets) }}\") )"
      - name: Test
        run: echo $USER_NPM_TOKEN
